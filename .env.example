DISCORD_TOKEN=your_token_here
DATABASE_PATH=data/bot.db
HOST=0.0.0.0
PORT=8000

# ── Code Runner (Piston sandbox) ──────────────────────────────
# URL del servicio Piston. En docker-compose ya está configurado
# como http://piston:2000 (resolución DNS interna de Docker).
# Solo cambia esto si Piston corre en otro host/puerto.
# PISTON_URL=http://piston:2000
